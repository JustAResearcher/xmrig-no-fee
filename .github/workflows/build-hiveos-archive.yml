name: Build HiveOS-compatible release archive

on:
  release:
    types: [created]
  workflow_dispatch: {}

jobs:
  build-and-upload:
    name: Build and upload tar.gz
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare release files
        run: |
          mkdir -p release
          cp xmrig config.json start.sh release/
          cp h-manifest.conf h-config.sh h-run.sh h-stats.sh release/
          chmod 0755 release/xmrig release/start.sh release/h-config.sh release/h-run.sh release/h-stats.sh
          # create tar.gz with preserved Unix permissions
          tar -C release -czf xmrig-no-fee.tar.gz .
          sha256sum xmrig-no-fee.tar.gz > SHA256SUMS

      - name: Upload to GitHub Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v1
        with:
          files: |
            xmrig-no-fee.tar.gz
            SHA256SUMS
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload to latest release (manual trigger)
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v1.0-no-fee-rebuilt
          files: |
            xmrig-no-fee.tar.gz
            SHA256SUMS
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
