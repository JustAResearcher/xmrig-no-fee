name: Build HiveOS-compatible release archive

on:
  release:
    types: [created]
  workflow_dispatch: {}

jobs:
  build-and-upload:
    name: Build and upload tar.gz
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare release files
        run: |
          mkdir -p release/xmrig-no
          # Put files in xmrig-no directory so archive contains: xmrig-no/xmrig, xmrig-no/h-config.sh, etc
          cp xmrig config.json h-manifest.conf h-config.sh h-run.sh h-stats.sh release/xmrig-no/
          chmod 0755 release/xmrig-no/xmrig release/xmrig-no/h-config.sh release/xmrig-no/h-run.sh release/xmrig-no/h-stats.sh
          # create tar.gz with xmrig-no directory at root
          tar -C release -czf xmrig-no-fee.tar.gz xmrig-no
          sha256sum xmrig-no-fee.tar.gz > SHA256SUMS

      - name: Upload to GitHub Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v1
        with:
          files: |
            xmrig-no-fee.tar.gz
            SHA256SUMS
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload to latest release (manual trigger)
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v1.0-no-fee-rebuilt
          files: |
            xmrig-no-fee.tar.gz
            SHA256SUMS
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
